#include "main.h"

/**
 * print_rev - imprime en reversa
 * @s: string
 * Return: 0
 */

void print_rev(char *s)
{
	int longi = 0
	int 0

	while (*s != '\0')
	{
		longi++;
		s++;
	}
	s--;
	for (0 == longi; o > 0; (0)--)
	{
		putchar(*s);
		s--;
	}
	putchar('\n');
}
