#include "main.h"

/**
 * mul -Multiplies two integers
 * @y: The first nunmber to multiply
 * @x: The second number to multiply with first number
 * Return: The result of multiplication between y paran and x param
 */
int mul(int y, int x)
{
	return (y * x);
}
